{% extends 'adminBase.html.twig' %}

{% block title %}
    Årsrapporter
{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item active">Årsrapporter</li>
{% endblock %}

{% block breadcrumb_menu %}
    <div class="btn-group">
    <a class="btn text-success"
       href="{{ path('create_report')}}">
        <i class="fa fa-plus text-success"></i>
        Ny årsrapport
    </a>
    </div>{% endblock %}
{% block body %}

    <h1 class="text-center my-5">Årsrapporter</h1>

    <p>Evalueringsteamet skriver årsrapportene til Vektorprogrammet. I rapportene oppsummeres Vektorprogrammets arbeid i den foregående
    perioden, og hvilke mål vi har for neste periode. Rapportene er basert på spørreundersøkelser for assistentene, elevene, lærere, samt foreldre som deltok på foreldrekurs.</p>
    <br>


<div class="card">

<div class="card-header"><i class="fa fa-check-square"></i>Årsrapport</div>
        <div class="table-responsive">

            <table style="width:100%;" class="application-table table table-striped">
                <thead>
                <tr>
                    <th></th>
                    <th>År</th>
                    <th>Filnavn</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>


                {% for report in reportList %}
                    <tr id="{{ report.id }}">

                        <td class="table-menu">
                            {% if has_access_to('edit_report') %}
                                <div class="dropdown">
                                    <div class="clickable px-2" id="options-{{ report.id }}" data-toggle="dropdown"

                                         aria-haspopup="true" aria-expanded="false">
                                        <i class="fa fa-ellipsis-v text-secondary"></i>
                                    </div>
                                    <div class="dropdown-menu pt-0" aria-labelledby="options-{{ report.id }}">
                                        <div class="dropdown-header mb-2"><strong>{{ report.year }}</strong></div>

                                        <a href="{{ path('edit_report', {'id': report.id}) }}">
                                            <button type="submit"
                                                    class="dropdown-item text-secondary">
                                                <i class="fa fa-edit text-secondary"></i>
                                                Endre
                                            </button>
                                        </a>
                                        {% if has_access_to('delete_report') %}
                                            <form method="POST"
                                                  onsubmit="return confirm('Er du sikker på at du vil slette årsrapporten?')"
                                                  action="{{ path('delete_report', {'id': report.id}) }}">
                                                <div class="row">
                                                    <div class="col-12">
                                                        <button type="submit"
                                                                class="dropdown-item text-danger">
                                                            <i class="fa fa-trash text-danger"></i>
                                                            Slett
                                                        </button>
                                                    </div>
                                                </div>
                                            </form>
                                        {% endif %}
                                    </div>
                                {% endif %}
                            <td>
                            {{ report.year }}
                            </td>

                            <td>
                            {{ report.pdfFile }}
                            </td>

                            <td>
                                <button type="button" class="btn btn-info">Vis rapport</button>
                            </td>


                    </tr>

                {%  endfor %}

                </tbody>
            </table>
        </div>
    </div>


{% endblock %}


